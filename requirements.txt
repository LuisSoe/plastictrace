# Core ML & Vision
torch
torchvision
opencv-contrib-python
numpy>=2.3.5  # Python 3.14 requires numpy 2.3.5+ (pre-built wheels)
              # Note: opencv-contrib-python may show dependency conflict but works fine
Pillow

# Web API & Server
flask==3.0.0
flask-cors==4.0.0

# Desktop GUI
PyQt5  # Hapus ==5.15.9 biar ambil latest binary
PyQtWebEngine  # Optional: for embedded map widget (install if map feature needed)